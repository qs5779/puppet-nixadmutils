#!/bin/bash
# -*- Mode: Bash; tab-width: 2; indent-tabs-mode: nil -*- vim:sta:et:sw=2:ts=2:syntax=sh#
#
# Revision History
#  20150301 - richarjt - initial version
#

SCRDIR=$(dirname "$0")

if [ -f "$1" ]
then
  while [ -f "$1" ]
  do
    svn add $1
    svnprop $1
    shift
  done
else
  echo "File not found: $1"
fi
